@model List<WebApplication1.Models.NewsModel>

@if (Model != null)
{
    @foreach (NewsModel news in Model)
    {
        <div class="news_div">
            <h3>
                @news.Summary
            </h3>
            <div class="text-center">
                <p>
                    @news.Text
                </p>
                <p>
                    @news.Published
                </p>
            </div>
        </div>
    }
}
else
{
    <div>
        <p>Nincs hír</p>
    </div>
}